<% if @_current_user %>
  <!-- Fixed navbar -->
  <nav class='nav-bar'>
    <div class='container'>
      <div class='navbar-header'>
        <!-- <a class='navbar-brand' href='#'>BCS PiSoc</a> -->
      </div>
      <div id='navbar' class='collapse navbar-collapse'>
        <ul class='navbar-nav'>
          <li class='<%= current_page?(news_index_path) ? 'active' : nil %>'>
            <%= link_to 'News', news_index_path %>
          </li>

          <li class='<%= current_page?(projects_path) ? 'active' : nil %>'>
            <%= link_to 'Projects', projects_path %>
          </li>

          <li class='<%= params[:controller] == 'users' ? 'active' : nil %>'>
            <a href='#' class='dropdown-toggle' data-toggle='dropdown' role='button' aria-expanded='false'>
              My User <span class='caret'></span>
            </a>
            <ul class='dropdown-menu' role='menu'>
              <li><%= link_to t('nav.logout'), logout_users_path %></li>
            </ul>
          </li>
        </ul>
      </div><!--/.nav-collapse -->
    </div>
  </nav>
<% end %>
